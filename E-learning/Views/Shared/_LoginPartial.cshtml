@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
  using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
  {
    @Html.AntiForgeryToken()

    @*<ul class="navbar-nav">
      @if (!User.IsInRole("Admin"))
      {
        <li class="nav-item">
          @Html.ActionLink("Profili im", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage", @class = "nav-link" })
        </li>
        <li class="nav-item">
          @Html.ActionLink("Timeline", "Timeline", "Artist", routeValues: null, htmlAttributes: new { title = "Manage", @class = "nav-link" })
        </li>
      }
      else
      {
        <li class="nav-item">
          @Html.ActionLink("Menaxho Kategorite", "Index", "Kategorias", routeValues: null, htmlAttributes: new { title = "Manage", @class = "nav-link" })
        </li>
      }

      <li><a class="nav-link" href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
    </ul>*@
  }
}
else
{
  <ul class="navbar-nav">
    <li class="nav-item">@Html.ActionLink("Regjistrohu", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink", @class = "nav-link" })</li>
    <li class="nav-item">@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink", @class = "nav-link" })</li>
  </ul>
}

